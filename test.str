let y = 69
let f = {
    fun () -> .MyInt(Int) {
        .MyInt(y)
    }
}

let g = fun () -> .MyInt(Int) {
    let y = 42

    f()
}

let objects: fn() -> Int = fun() -> Int {
    const let x = 7

    // dump(.hello(42, "wow"))
        
    x
}

objects()
g()

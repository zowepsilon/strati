const id = fun (T: Type) {
    fun (x: T) -> T {
        x
    }
}

const idint = id(Int)

let th = const { idint }

const t = 0
let t = th(const { t })

const { quote {
    42
    print("hello")
} }

id(Int)(t)

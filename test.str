const y = 69
const f = {
    fun() -> .MyInt(Int) {
        .MyInt(y)
    }
}

let g = fun() -> .MyInt(Int) {
    let y = 42

    f()
}

const { f() }

const identity = const fun(x: .) -> . { x }

//const id = const fun (T: Type) -> . {
//    fun (x: T) -> T { x }
//}


let objects: fn() -> Int = fun() -> Int {
    const x = 7
    const y = x
    // dump(.hello(42, "wow"))
    
    const { f() }

    identity(.Thing(42))

    x
}

objects()
g()

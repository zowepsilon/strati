const id = fun ($T: Type) {
    fun (x: T) -> T {
        x
    }
}

const $x = id(Int)

let th = const { idint }

const t = 0
let t = th(const { t })

$hello

add($woo, $it, $works)

const { quote {
    let $x = 42
    print("hello")
} }

id(Int)(t)
